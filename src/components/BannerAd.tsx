// AdMob banner is rendered natively by the SDK at the bottom of the screen.
// On web we render nothing — placeholder banners have been removed per spec.
export const BannerAd = (_: { className?: string }) => null;
